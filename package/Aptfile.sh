# This file will run commands to install various apps

sudo apt-get update			# Refresh package list
apt-get install build-essentials	# Various compilers (GCC, etc.)
apt-get install wget curl		# Utils for fetching resources over network
apt-get install zsh			# Z shell

